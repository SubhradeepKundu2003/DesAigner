package com.tcs.contentGenerator.agent.graphics;

/** Where a placed image came from — for transparency in the {@link GraphicsReport}. */
public enum ImageSource {
    SOURCE_DOCUMENT,
    BRAND_ASSET
}
